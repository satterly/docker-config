version: '2'

services:
  primary:
    image: mongo
    command: mongod --replSet rs0 --smallfiles
    ports:
      - 27017
    volumes:
      - /data/db
      - /data/configdb
    restart: always
    networks:
      - mongonet

  secondary:
    image: mongo
    command: mongod --replSet rs0 --smallfiles
    volumes:
      - /data/db
      - /data/configdb
    networks:
      - mongonet

networks:
  mongonet:
    driver: bridge
